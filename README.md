# LOUGHA PLUS — Landing General 2

Même structure que General (6mois), palette Navy / Gold LOUGHA PLUS.

## EasyPanel
- Root: `.` (dans le monorepo: Dockerfile Path = `Dockerfile.general2`)
- Port: 3000
